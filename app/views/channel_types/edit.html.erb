<% content_for(:title) do%>
	<%= 'Editing Channel Type: ' + @channel_type.name + ': Sky Clarity Media Management' %>
<% end %>

<div class='title-episode-bold'><b>Editing Channel Type</b></div>

<%= render 'form' %>

<p>
  <%= link_to 'Show', @channel_type %> |
  <%= link_to 'Back', :back %>
</p>
