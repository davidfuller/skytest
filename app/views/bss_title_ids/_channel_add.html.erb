<div class="panel-group">
  <div class="panel panel-default">
    <div class="panel-heading">
      <h3 class="panel-title title-episode-bold">
        <a data-toggle="collapse" href="#channels-add">Add Channel</a>
      </h3>

      <% if @bss_title_id.channel_add_show %>
        <div id="channels-add" class="panel-collapse collapse in">
      <% else %>
        <div id="channels-add" class="panel-collapse collapse">
      <% end %>

        <%= render 'search', path: @bss_title_id, remote: true, search: :search %>
        <br>
        <div id = 'bss-title-id-channels'>
          <%= render 'channels'%>
        </div>
      </div>
    </div>
  </div>
</div>