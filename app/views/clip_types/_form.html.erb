<%= form_for(@clip_type) do |f| %>
  <% if @clip_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clip_type.errors.count, "error") %> prohibited this clip_type from being saved:</h2>

      <ul>
      <% @clip_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :default_duration %><br>
    <%= f.number_field :default_duration %>
  </div>
  <div class="field">
    <%= f.label :default_has_audio %><br>
    <%= f.check_box :default_has_audio %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
